# Support

- Use GitHub Issues for reproducible bugs and bounded feature requests.
- Use GitHub Discussions for product questions and host-adapter design.
- Use private vulnerability reporting for security issues.

Before filing a bug, run `npm run validate` and include the redacted output,
host type and version, operating system, plugin version, adapter version if
applicable, installation method, and whether the transition was manual
compaction, automatic compaction, resume, clear, or Agent handoff. For macOS,
also report whether Codex was launched from Finder/Dock or a terminal and
whether `node --version` was visible to that process.

Never attach raw transcripts, task ledgers, credentials, or private project data.
